---
name: recruit-ops
description: >-
  招聘辅助工具，专为 HR 场景设计。当用户提到 pretalk、笔记、候选人、面试反馈、
  面试评价、招聘数据、候选人追踪、面试安排等关键词时触发。
  核心能力：1) 将随手记录的 pretalk 笔记按统一模板格式化；
  2) 后续可扩展面试反馈整理、候选人对比表、招聘数据汇总等功能。
  这是一个纯指令式 skill，无需额外脚本或依赖。
---

# Recruit Ops — 招聘辅助工具

## Pretalk 笔记格式化（核心功能）

将 HR 随手记录的 pretalk（电话初面）笔记，自动整理为统一格式的候选人卡片。

### 输入格式

无格式限制，自由文本即可。不限分行、逗号、段落：

```
张三 腾讯3年 后端 Go 在职 转岗出来看看 薪资60w 期望70w以内 目前没有别的流程
```

```
李四 字节跳动2年，产品经理，已离职，因为业务线被砍，之前是P6，现在45w期望55w，
目前在面美团和拼多多，说对我们挺感兴趣
```

### 输出模板

每个候选人输出以下结构，字段顺序固定：

```
基本信息：<姓名> | <公司/年限> | <方向> | <技术栈/产品线>
是否在职：是 / 否
看机会原因：<一段简短概括>
职级：<职级> / 待确认
薪况：<原始薪资形式>（<年包W>），如 35K×14（49W）或 40W
期望涨幅：<百分比> / 待确认（有期望薪资时自动计算）
目前的面试流程：<其他公司在面情况> / 无
对公司意向度：高 / 中 / 低 / 待确认
```

### 提取规则

| 字段 | 提取策略 | 示例 |
|------|---------|------|
| 基本信息 | 姓名(2-4字中文)+公司名+年限数字+方向关键词 | "字节跳动2年" → 字节跳动 2年 |
| 是否在职 | 匹配在职/离职/待业/被裁/已离职等明确关键词。**无明确关键词时标待确认，不能从"看机会/想看看"推断为在职** | "已离职" → 否，"想看看机会" → 待确认 |
| 看机会原因 | 句子中的原因从句（因为/由于/想/希望等） | "业务线被砍" |
| 职级 | 数字+字母组合（P6/2-1/高级/资深等）。**多段经历有多个职级时全部保留用 / 分隔** | "P6" → P6，"阿里P7后来字节2-2" → P7 / 2-2 |
| 薪况 | 数字+w/k/万/年薪 等 | "40w" → 40W，"年薪40万" → 40W |
| 期望涨幅 | 由期望薪资-当前薪资格式计算，或直接提取百分比 | "期望50w，当前40w" → 25% |
| 面试流程 | 公司名（字节/美团/拼多多/阿里等）+语境词 | "在面美团" → 美团（面试中） |
| 意向度 | 语义判断：感兴趣/看看/无所谓/很想去等 | "挺感兴趣" → 高 |

### 缺省与推断规则

笔记中没提到的字段，**一律标"待确认"**，不允许自行推测或编造。特别是：

- **是否在职**：只有原文出现"在职/离职/待业/被裁"等明确词时才判定。只说"看机会/想看看/想出来看看"等，一律标"待确认"
- **职级**：多段经历提到多个职级时全部保留，用 / 分隔。不能说"选后一个"或"只保留最新的"
- **意向度**：原文没有明确态度词时标"待确认"，不能从"先看看机会"推断为"低"

例如：

```
基本信息：王五 | 待确认 | 后端 | Go
是否在职：待确认
看机会原因：待确认
```

### 薪况换算规则

薪况字段**保留原始表达形式，同时在括号内标注年包 W 值**：

- "40w" / "40万" / "年薪40万" → **40W（40W）**
- "25k" → **25K（30W）**（月薪，自动推算年包）
- "35k*14薪" → **35K×14（49W）**（保留原始乘式）
- "月薪3万" → **月薪3万（36W）**
- "30w，期望36w" → 当前 **30W（30W）**，期望 **36W（36W）**
- 有月薪+薪数月数时（如"35k*14薪 期望40k"）：当前 **35K×14（49W）**，期望月薪 40k→年包 **56W**，涨幅约 14%
- 不明确的数字（如只说"40"不提单位）→ **40？** 并标注"单位待确认"

当原始表达本身就是 W 单位时（如"40w""30万"），括号里可以不重复写相同值。

### 特殊处理

- 薪资范围："40-50w" → 当前 40W，期望 50W，期望涨幅 25%
- 多个公司："在面美团和拼多多" → 美团、拼多多（面试中）
- 已拿 Offer："已经拿了字节的 offer" → 字节（已拿 Offer）
- 多个职级："阿里P7后来字节2-2" → P7 / 2-2
- 电话号码 / 微信 / 邮箱 → 自动保留在基本信息末尾

## 即时模式

用户一句话即可触发，不需要先问"要格式化吗"：

```
pretalk: 张三 腾讯3年 后端 Go 在职 转岗出来看看 薪资60w
```

直接输出格式化结果，无需额外确认。

## 批量模式

多条笔记用空行分隔，批量输出：

```
pretalk:
张三 腾讯3年 后端 Go 在职...
李四 字节2年 产品 已离职...
```

逐条输出，每条前面加序号和空行分隔。

## 反例

1. **不要编造信息** — 笔记中没提到的字段必须标"待确认"，不能根据上下文推测
2. **不要改变原文事实** — 公司名、年限、薪资等数字必须原文保留，不能四舍五入或改写
3. **不要问多余问题** — 用户输入"pretalk: ..."后直接输出结果，不需要确认"是否要格式化"或"还有别的吗"
4. **不要漏字段** — 8 个字段必须全部输出，缺失的用"待确认"，不能跳过
5. **不要输出额外内容** — 只输出格式化结果，不输出分析过程、JSON、或解释性文字